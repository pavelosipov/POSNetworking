source 'https://github.com/CocoaPods/Specs.git'

platform :ios, '7.0'
workspace 'POSRx'

target "POSRx" do
end

target "POSRxTests" do
    pod 'POSRx', :path => '.'
    pod 'POSAllocationTracker'
end
